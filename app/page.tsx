/* This file redirects users into the auth flow or dashboard via auth provider. */
export default function HomePage() {
  return <main className="p-6 text-sm text-slate-600">Loading workspace...</main>;
}
